### Env Set up
- Get Spark set up locally with Docker
  - `cd ./docker-spark`
  - `docker-compose up`
- Install requirements
  - `pip install -r ./requirements.txt`
- Run app with Python
  - `python ./app/app.py`